services:
  - type: web
    name: alphapulse
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: MODEL_URL
        sync: false   # Set this manually in the Render dashboard (HuggingFace direct download URL)
